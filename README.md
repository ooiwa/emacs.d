# how to install

```
$ git clone git@github.com:ooiwa/emacs.d.git ~/.emacs.d
$ curl -fsSL https://raw.githubusercontent.com/cask/cask/master/go | python
$ echo 'export PATH=$HOME/.cask/bin:$PATH' >> ~/.zshrc
$ source ~/.zshrc
$ cd ~/.emacs.d
$ cask install
```
